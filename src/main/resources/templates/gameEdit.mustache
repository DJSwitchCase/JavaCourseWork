<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Страница редактирования товаров</title>
    <link rel="stylesheet" href="static/styles/style.css">
</head>
<body>
{{#game}}
    <form action="/admin/games/{{id}}" method="post">

        <div>
            <h1 id="style_h1">Редактирование имени</h1>
            <input type="text" name ="name" value="{{name}}">
            <input type="text" name="creationDate" value="{{creationDate}}">
        </div>
        <input type="hidden" value="{{id}}" name="gameId">
{{/game}}
    <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
    <button type="submit">Сохранить изменения</button>
</form>
</body>
</html>