<!DOCTYPE html>
<htmL>
<head>
    <meta charset="UTF-8">
    <title>Страница администратора</title>
</head>
<body>
<div>
<form method="post">
    <input type="text" name="name" placeholder="Введите название игры" />
    <input type="number" name="creationDate" placeholder="Введите дату создания игры">
    <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
    <button type="submit">Добавить в БД</button>
</form>
</div>
<div>
<form action="/logout" method="post">
    <input type="submit" value="Sign out"/>
    <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
</form>
</div>
Фильтр:
<form method="post" action="/admin/filter">
   <input type="text" name="filter">
    <input type="hidden" name="_csrf" value="{{_csrf.token}}" />
</form>
    Список игр:
    <table>
        <thead>
        <th>ID</th>
        <th>Имя</th>
        <th>Дата</th>
        </thead>
        <tbody>
    {{#messages}}
        <tr>
        <td>{{id}}</td>
        <td>{{name}}</td>
        <td>{{creationDate}}</td>
        <td><a href ="/admin/games/{{id}}">Редактировать</a></td>
        </tr>
    {{/messages}}
    </tbody>
    </table>
Список пользователей:
<table>
    <thead>
    <th>Имя</th>
    <th>Роль</th>
    <th>Статус</th>
    </thead>
    <tbody>
    {{#users}}
        <tr>
            <td>{{username}}</td>
            <td>{{roles}}</td>
            <td>{{active}}</td>
            </td>
            <td><a href="/admin/{{id}}">Редактировать</a></td>
        </tr>
    {{/users}}
    </tbody>
</table>
</body>
</html>